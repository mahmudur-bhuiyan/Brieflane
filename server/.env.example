# App
PORT=4000
JWT_SECRET=change-me-in-production
# Optional override for encrypting stored ActiveCollab passwords (defaults to JWT_SECRET)
# CREDENTIALS_ENCRYPTION_KEY=
WEB_ORIGIN=http://localhost:5173

# Neon (Step 3)
DATABASE_URL=postgresql://user:password@host/brieflane?sslmode=require

# Bootstrap Super Admin (Step 4)
BOOTSTRAP_ADMIN_EMAIL=admin@brieflane.local
BOOTSTRAP_ADMIN_PASSWORD=admin123456
