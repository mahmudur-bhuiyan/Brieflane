# Copy to project root as `.env` for Prisma CLI, or to `apps/api/.env` for the API server.
# Both locations are loaded by the API at startup.

DATABASE_URL=postgresql://user:password@host/brieflane?sslmode=require
